---
import BaseLayout from "./BaseLayout.astro";
const { frontmatter } = Astro.props;
---

<BaseLayout pageTitle={frontmatter.title}>
    
    <p>Written by {frontmatter.author}</p>
    <p>publié le {frontmatter.pubDate}</p>
    <slot />

</BaseLayout>
